LISTENER =
  (DESCRIPTION_LIST =
    (DESCRIPTION =
      (ADDRESS = (PROTOCOL = TCP)(HOST = {{ ansible_host }})(PORT = 1521))
      (ADDRESS = (PROTOCOL = IPC)(KEY = EXTPROC1521))
    )
  )

SID_LIST_LISTENER =
  (SID_LIST =
    (SID_DESC =
      (GLOBAL_DBNAME = {{ 'PROD' if inventory_hostname == groups['primary'][0] else 'PROD_SBY' }}.localdomain)
      (ORACLE_HOME = {{ oracle_home }})
      (SID_NAME = PROD)
    )
    (SID_DESC =
      (GLOBAL_DBNAME = {{ 'PROD' if inventory_hostname == groups['primary'][0] else 'PROD_SBY' }}_DGMGRL.localdomain)
      (ORACLE_HOME = {{ oracle_home }})
      (SID_NAME = PROD)
    )
  )

